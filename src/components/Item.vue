<template>
  
</template>
<script setup>
/* import { ref, onMounted } from "vue";
import { fetchItem, fetchItemsRange } from "@/services/items.service";

const presItem = ref(true);

onMounted(async () => {
  await fetchItem();
}); */
</script>
